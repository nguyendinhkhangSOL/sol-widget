# ──────────────────────────────────────────────────────────────────────────
#  SOL WIDGET (frontend) — biến môi trường (build-time)
#  Sao chép thành `.env` (local) hoặc `.env.production` (deploy Firebase).
# ──────────────────────────────────────────────────────────────────────────


# URL của backend.
# Local:        http://localhost:4000
# Production:   https://sol-backend.onrender.com
VITE_API_BASE=http://localhost:4000
