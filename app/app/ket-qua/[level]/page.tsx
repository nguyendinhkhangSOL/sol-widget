// DEPRECATED — file này phải xoá khỏi VPS (deploy.ps1 đã exclude)
// Nếu file này nằm trong build, Next.js sẽ fail vì conflict với [cohort]
// Không sử dụng nội dung này — chỉ giữ tránh empty file lỗi git
export default function Deprecated() { return null; }
